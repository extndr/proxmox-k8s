---
apiVersion: v1
kind: Secret
metadata:
  name: postgres
type: Opaque
stringData:
  # Deliberately non-sensitive lab credential. Never commit a real password here.
  POSTGRES_PASSWORD: demo-password
